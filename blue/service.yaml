apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: flags
spec:
  type: NodePort
  selector:
    app: my-app
    version: blue
  ports:
    - name: http
      protocol: TCP
      targetPort: 5000
      nodePort: 30080
